export interface AllComponentProps {
    visible?: boolean;  // Optional prop for visibility
    className?: string;  // Optional prop for className
    label?: string;
    placeholder?: string;
    disabled?: boolean ;
    loading?: boolean ;
}